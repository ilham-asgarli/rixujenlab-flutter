import 'package:flutter/material.dart';

class MyTypeView extends StatelessWidget {
  final MyTypeView myTypeView;

  const MyTypeView({
    super.key,
    required this.myTypeView,
  });

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
