enum SizeProperty { width, height }

enum Entry {
  signIn,
  signUp,
  guess,
}

enum MainMenu {
  skinType,
  dictionary,
  tips,
  routine,
}

enum SignUpField {
  fullName,
  userName,
  email,
  password,
}

enum SignInField {
  email,
  password,
}

enum RoutinesSteps {
  cleanser,
  toner,
  faceSerum,
  moisturizer,
  eyeCream,
  sunScreen,
}

enum SkincareDictionary {
  takeAPhoto,
  uploadPhoto,
  search,
}

enum SkinType {
  sensitive,
  dry,
  oily,
  combination,
  normal,
}

enum TypeFeature {
  definition,
  care,
  nutrition,
}

enum MainDrawer {
  signIn,
  signUp,
  testResults,
}
